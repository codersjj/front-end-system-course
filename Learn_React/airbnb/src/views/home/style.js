import styled from "styled-components";

export const HomeWrapper = styled.div`
  > .content {
    margin: 0 auto;
    width: 1032px;
  }

  .good-price {
    margin-top: 30px;

    .room-list {
      display: flex;
      flex-wrap: wrap;
      margin: 0 -8px;
    }
  }
`